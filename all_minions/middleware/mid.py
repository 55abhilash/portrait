
class tmp_middleware(object):
    # To make the middleware active
    # We programatically comment 'pass'
    # and uncomment the function below
    pass
    #def process_response(self, request, response):
        #if(request.url.contains('all_minions'):
        # some more stuff
        # return response
